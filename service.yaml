apiVersion: v1
kind: Service
metadata:
  name: demo-api
  namespace: autonomic-demo
  labels:
    app: demo-api
spec:
  selector:
    app: demo-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
